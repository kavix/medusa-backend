# Exclude development and build files
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Note: database.db included as pre-built database to avoid compilation issues

# Exclude version control
.git/
.gitignore

# Exclude documentation and README files
README.md
*.md

# Exclude test and development files
test-output.log
*.log
logs/

# Exclude GitHub Actions (not needed in container)
.github/

# Exclude environment files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Exclude Docker files (avoid recursion)
Dockerfile
docker-compose.yml
.dockerignore

# Exclude IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Exclude OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Exclude temporary files
tmp/
temp/
*.tmp

# Exclude coverage reports
coverage/
*.lcov
.nyc_output

# Note: dist/ included as it contains pre-built frontend assets
build/

# Exclude shell scripts (not needed in container)
restart.sh
test-login.html
.azure/